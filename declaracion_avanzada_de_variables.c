#include <stdio.h>

//(comentario de una sola linea)
extern int a, b, c;
//Declarando las variables
float f, g, h;

int main()
{
//Definición de variables
int a, b, c;
//inicializando las variables
    g = 1000.999999;
    f = 1.111111;

    h = g + f;
    printf("El valor de la suma es: %f",h);
    return 0;
}